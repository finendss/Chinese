local Translations = {
已将所有中文方括号【 】替换为英文方括号[ ]，修改后的文本如下：

[“Azure Hub|Ink Game”]=“Azure Hub|墨水游戏“,
[“Xenoz”]=“FIN Team 汉化”,
[“RedLightGreenLight”]=“红灯绿灯”,
[“Remove Red Door”]=“移除红门”,
[“Remove red door at start”]=“开始时移除红门”,
[“Tp To End”]=“传送至终点”,
[“Teleport to end”]=“传送至终点”,
[“Anti Injury”]=“防受伤”,
[“Anti injury [ turn on anti stun for no slow ]”]=“防受伤[开启防眩晕以避免减速]”,
[“Unlock Dash & Speed”]=“解锁冲刺与加速”,
[“Sprint faster, unlock dash ability”]=“跑得更快，解锁冲刺能力”,
[“Help Random”]=“随机帮助”,
[“External GUI”]=“外部图形界面”,
[“Troll Random”]=“随机捣乱”,
[“Dalgona”]=“糖饼”,
[“God Mode”]=“上帝模式”,
[“Make your cookie unbreakable”]=“让你的糖饼不会碎”,
[“Finish Shape”]=“完成形状”,
[“Instantly finish your cookie”]=“立即完成你的糖饼”,
[“Light Off”]=“关灯”,
[“Save Position”]=“保存位置”,
[“Save before teleporting anywhere”]=“在传送到任何地方之前保存”,
[“Teleport To Saved Position”]=“传送到保存的位置”,
[“Teleport to your saved position”]=“传送到你保存的位置”,
[“Teleport Inside Pig”]=“传送到猪内部”,
[“Teleport inside the prize pool pig”]=“传送到奖池猪内部”,
[“Hide And Seek”]=“捉迷藏”,
[“ESP”]=“绘制”,
[“Highlight all players and their role”]=“高亮显示所有玩家及其角色”,
[“Teleport To Exit Door”]=“传送到出口门”,
[“Teleport to exit door”]=“传送到出口门”,
[“Follow Closest Hider”]=“跟随最近的躲藏者”,
[“Teleport to closest hider [ spam ur knife to kill them ]”]=“传送到最近的躲藏者[狂按 knife 杀死他们]”,
[“Tug Of War”]=“拔河”,
[“Auto Pull Rope”]=“自动拉绳”,
[“Auto pull the rope”]=“自动拉绳”,
[“Rope Game”]=“绳游戏”,
[“Be Able To Run”]=“能够跑步”,
[“Be able to run during rope game ( toggle anti stun for this to work )”]=“在绳游戏中能够跑步（开启防眩晕以使其生效)”,
[“Delete Rope”]=“删除绳子”,
[“Delete the rope”]=“删除绳子”,
[“Glass Bridge”]=“玻璃桥”,
[“Show Correct Path”]=“显示正确路径”,
[“Show correct path for glass game”]=“显示玻璃游戏的正确路径”,
[“Mingle”]=“蓄力迷你游戏”,
[“Auto Power Hold”]=“自动蓄力”,
[“Automatically completes the Mingle power hold minigame/QTE”]=“自动完成 Mingle 蓄力迷你游戏/快速反应事件”,
[“Player”]=“玩家”,
[“Unlock PhantomStep”]=“解锁幻影步”,
[“Unlock phantomstep ability, cannot change to old dash ability.”]=“解锁幻影步能力，无法切换回旧的冲刺能力”,
[“Jump Higher ( 50 = default )”]=“跳得更高（50 = 默认）”,
[“Run Faster”]=“跑得更快”,
[“Anti Fling”]=“防甩飞”,
[“Anti fling, cannot be disabled”]=“防甩飞，无法禁用”,
[“Anti Ragdoll”]=“防布娃娃（游戏中角色失去控制的状态）”,
[“Anti ragdoll”]=“防布娃娃状态”,
[“Anti Stun”]=“防眩晕”,
[“Anti stun”]=“防眩晕”,
}

local function translateText(text)
    if not text or type(text) ~= "string" then return text end
    
    if Translations[text] then
        return Translations[text]
    end
    
    for en, cn in pairs(Translations) do
        if text:find(en) then
            return text:gsub(en, cn)
        end
    end
    
    return text
end

local function setupTranslationEngine()
    local success, err = pcall(function()
        local oldIndex = getrawmetatable(game).__newindex
        setreadonly(getrawmetatable(game), false)
        
        getrawmetatable(game).__newindex = newcclosure(function(t, k, v)
            if (t:IsA("TextLabel") or t:IsA("TextButton") or t:IsA("TextBox")) and k == "Text" then
                v = translateText(tostring(v))
            end
            return oldIndex(t, k, v)
        end)
        
        setreadonly(getrawmetatable(game), true)
    end)
    
    if not success then
        warn("元表劫持失败:", err)
       
        local translated = {}
        local function scanAndTranslate()
            for _, gui in ipairs(game:GetService("CoreGui"):GetDescendants()) do
                if (gui:IsA("TextLabel") or gui:IsA("TextButton") or gui:IsA("TextBox")) and not translated[gui] then
                    pcall(function()
                        local text = gui.Text
                        if text and text ~= "" then
                            local translatedText = translateText(text)
                            if translatedText ~= text then
                                gui.Text = translatedText
                                translated[gui] = true
                            end
                        end
                    end)
                end
            end
            
            local player = game:GetService("Players").LocalPlayer
            if player and player:FindFirstChild("PlayerGui") then
                for _, gui in ipairs(player.PlayerGui:GetDescendants()) do
                    if (gui:IsA("TextLabel") or gui:IsA("TextButton") or gui:IsA("TextBox")) and not translated[gui] then
                        pcall(function()
                            local text = gui.Text
                            if text and text ~= "" then
                                local translatedText = translateText(text)
                                if translatedText ~= text then
                                    gui.Text = translatedText
                                    translated[gui] = true
                                end
                            end
                        end)
                    end
                end
            end
        end
        
        local function setupDescendantListener(parent)
            parent.DescendantAdded:Connect(function(descendant)
                if descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox") then
                    task.wait(0.1)
                    pcall(function()
                        local text = descendant.Text
                        if text and text ~= "" then
                            local translatedText = translateText(text)
                            if translatedText ~= text then
                                descendant.Text = translatedText
                            end
                        end
                    end)
                end
            end)
        end
        
        pcall(setupDescendantListener, game:GetService("CoreGui"))
        local player = game:GetService("Players").LocalPlayer
        if player and player:FindFirstChild("PlayerGui") then
            pcall(setupDescendantListener, player.PlayerGui)
        end
        
        while true do
            scanAndTranslate()
            task.wait(3)
        end
    end
end

task.wait(2)

setupTranslationEngine()

local success, err = pcall(function()
--这下面填加载外部脚本
loadstring(game:HttpGet("https://raw.githubusercontent.com/ScripterVaz/InkGameAzure/main/Protected_7974105512464815.lua.txt"))()


end)

if not success then
    warn("加载失败:", err)
end
